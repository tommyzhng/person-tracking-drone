CHECK IP ADDRESS!!

mission planner: 
cd ~/missionplanner;mono MissionPlanner.exe

Workflow:

Run Simulation
Terminal 1
gazebo --verbose ~/ardupilot_gazebo/worlds/iris_arducopter_runway.world

Terminal 2
cd ~/ardupilot/ArduCopter/
sim_vehicle.py -v ArduCopter -f gazebo-iris --console
or
sim_vehicle.py -v ArduCopter -L SCHOOL 

if script is not connecting:
type "output" to see what addresses mavlink messages are sent to
"output add" current ip address from ipconfig (cmd) using port :14550